name: plocate
version: 1.1.22
description: plocate - fast file locator
license: GPL-2.0-or-later
homepage: https://plocate.sesse.net/
source_url: https://plocate.sesse.net/download/plocate-1.1.22.tar.gz
build:
  build_system: Autotools
  build_commands:
  - ./configure --prefix=/usr
  - make
dependencies:
  build_dependencies:
  - gcc
  - make
  host_dependencies: []
  runtime_dependencies:
  - glibc
bootstrap:
  enabled: false
changelog:
- version: 1.1.22
  date: '2026-01-03'
  author: Oreon Project Team
  email: packaging@oreonproject.org
  changes:
  - Initial package for Oreon 11
