name: alsa-utils
version: "1.2.13"
description: "ALSA utilities"
author: "Various"
license: "GPL-2.0-or-later"
homepage: "https://www.alsa-project.org/"
repository: ""
source_url: "https://www.alsa-project.org/files/pub/utils/alsa-utils-1.2.13.tar.bz2"
keywords:
  - alsa-utils
  - audio
  - multimedia
categories:
  - multimedia
  - audio

dependencies:
  build_dependencies:
    - {name: "gcc", version_constraint: ">=11", optional: false}
    - {name: "make", version_constraint: ">=4.3", optional: false}
    - {name: "pkgconf", version_constraint: ">=1.7", optional: false}
    - {name: "alsa-lib", version_constraint: ">=1.2.13", optional: false}
  runtime_dependencies:
    - {name: "glibc", version_constraint: ">=2.30", optional: false}
    - {name: "alsa-lib", version_constraint: ">=1.2.13", optional: false}
  optional_dependencies: []
  conflicts: []

build:
  build_system: Make
  build_commands:
    - |
      export PKG_CONFIG_PATH="/usr/lib/pkgconfig:/usr/lib64/pkgconfig:/usr/share/pkgconfig"
      # Try to find alsa headers in various locations
      ALSA_INCLUDE=""
      for dir in /usr/include /usr/local/include /opt/alsa/include; do
        if [ -f "$dir/alsa/asoundlib.h" ]; then
          ALSA_INCLUDE="$dir"
          echo "Found ALSA headers in $dir"
          # Try to get version
          if [ -f "$dir/alsa/version.h" ]; then
            ALSA_VERSION=$(grep -E '^#define[[:space:]]+SND_LIB_VERSION_STR' "$dir/alsa/version.h" | sed 's/.*"\(.*\)".*/\1/' | head -1)
            if [ -n "$ALSA_VERSION" ]; then
              echo "Found ALSA version: $ALSA_VERSION"
            fi
          fi
          break
        fi
      done
      # If headers not found, try to use the library anyway
      if [ -z "$ALSA_INCLUDE" ]; then
        echo "Warning: ALSA headers not found, using fallback"
        ALSA_INCLUDE="/usr/include"
      fi
      export ALSA_CFLAGS="-I$ALSA_INCLUDE"
      export ALSA_LIBS="-lasound -lm -lpthread"
      export CPPFLAGS="-I$ALSA_INCLUDE"
      export LDFLAGS="-L/usr/lib -L/usr/lib64"
      ./configure --prefix=/usr CPPFLAGS="$CPPFLAGS" LDFLAGS="$LDFLAGS" ALSA_CFLAGS="$ALSA_CFLAGS" ALSA_LIBS="$ALSA_LIBS"
    - "make -j$(nproc)"
  build_dependencies:
    - "gcc"
    - "make"
    - "pkgconf"
    - "alsa-lib"
  build_flags: []
  environment:
    CFLAGS: "-O2 -pipe"
    CPPFLAGS: "-I/usr/include"
    LDFLAGS: "-L/usr/lib -L/usr/lib64"
    PKG_CONFIG_PATH: "/usr/lib/pkgconfig:/usr/lib64/pkgconfig:/usr/share/pkgconfig"
  working_directory: null
  target_architectures: [X86_64v1, X86_64v3, Aarch64]
  cross_compiler_prefix: null
  target_sysroot: null

install:
  install_method: RunCommands
  install_commands:
    - "make install DESTDIR=$DESTDIR"
  install_directories: ["/usr/lib", "/usr/bin"]
  install_files: []
  post_install_commands:
    - "ldconfig -n $DESTDIR/usr/lib || true"

files:
  include_patterns: ["**/*.c", "**/*.h"]
  exclude_patterns: ["**/*.o"]
  binary_files: []
  config_files: []
  documentation_files: []
  license_files: []

scripts:
  pre_install: "echo 'Installing alsa-utils...'"
  post_install: |
    echo 'alsa-utils installed'
    ldconfig 2>/dev/null || true
  pre_uninstall: null
  post_uninstall: "ldconfig 2>/dev/null || true"
  pre_upgrade: null
  post_upgrade: "ldconfig 2>/dev/null || true"

metadata:
  maintainer: "Oreon Project Team <packaging@oreonproject.org>"
  section: "multimedia"
  priority: "optional"
  provides:
    - "alsa-utils"
  conflicts: []
